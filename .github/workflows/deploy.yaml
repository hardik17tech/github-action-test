name: CI/CD for React App

on:
  push:
    branches: prod_1.1.1
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: Install dependencies
        run: npm install

      - name: Build Project
        run: npm run build
        
      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@4.1.0
        with:
          branch: gh-pages
          folder: build

# name: CI/CD for React App

# on:
#   push:
#     branches:
#       # - 'prod_*' # Only trigger for prod version branches (ex: prod_1.1.1)
#       - 'wip-hardik'
#   workflow_dispatch:

# permissions:
#   contents: write

# jobs:
#   build_and_deploy:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout repository
#         uses: actions/checkout@v3

#       - name: Setup Node.js
#         uses: actions/setup-node@v3
#         with:
#           node-version: 18

#       - name: Install dependencies
#         run: npm install

#       - name: Build Project
#         run: npm run build

#       - name: Deploy to GitHub Pages
#         uses: JamesIves/github-pages-deploy-action@4.1.0
#         with:
#           branch: gh-pages       # Production deployment branch
#           folder: build          # React build folder
#           clean: true            # Cleans old files before deploy
#           target-folder: ${{ github.ref_name }}  # Deploy in a versioned folder
